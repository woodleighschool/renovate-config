# https://mise.jdx.dev/configuration.html
[tools]
actionlint = "1.7.12"
lefthook = "2.1.11"
node = "26.7.0"
oxfmt = "0.64.0"
"npm:renovate" = { version = "44.38.0", trust_policy_excludes = [
  "@yarnpkg/libzip",
] }
shellcheck = "0.11.0"
zizmor = "1.29.0"

[tasks.lint]
description = "Check JSON/JSON5, YAML, Markdown, and shell formatting"
run = [
  "oxfmt --check .",
  "git ls-files -co --exclude-standard '*.sh' | xargs -r shellcheck",
]

[tasks.validate]
description = "Validate Renovate presets against the schema"
run = "renovate-config-validator --strict"

[hooks]
postinstall = "lefthook install"
